pipeline {
  agent any
  stages {
    stage('Clonar repositorio') {
      steps {
          git branch: 'Develop', url: 'https://github.com/JorchCanelo/Proyecto-DevOps-2023.git'
        }
      }   
    stage('Instalar dependencias') {
      steps {
        powershell 'npm install'
      }
    }
    stage('Ejecutar pruebas') {
      steps {
        powershell 'npm test -- --forceExit' 
      }
    }
    stage('Detener contenedores'){
      steps{
        powershell 'docker stop $(docker ps -aq)'
      }
    }
    stage('Desplegar') {
      steps {
        build job: 'Proyecto-Final-Devops-Despliegue/Jenkinsfile-deploy', propagate: true
      }
    }
  }
}
